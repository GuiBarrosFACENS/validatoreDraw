1. testExecute_ValidInvoice_NoExceptionThrown:

Verifica se não há exceção lançada para uma fatura válida.
Define o status de pagamento como verdadeiro e o valor pago como um BigDecimal positivo.
Executa o método execute da classe ValidateBilledInvoiceYKCommand com a fatura válida.
Não espera que ocorra uma exceção.

2. testExecute_InvalidInvoice_ThrowsValidationException:

Verifica se uma exceção de validação é lançada para uma fatura inválida.
Define o status de pagamento como falso e o valor pago como zero.
Executa o método execute com a fatura inválida.
Espera-se que uma exceção de ValidationException seja lançada.

3. testCreateMessage_WithViolations:

Testa a criação de uma mensagem de exceção com violações.
Define um mapa de violações com alguns campos e mensagens de erro.
Chama o método createMessage da classe ValidateBilledInvoiceYKCommand.
Verifica se a mensagem de exceção contém as mensagens de erro esperadas.

4. testCreateMessage_NoViolations:

Testa a criação de uma mensagem de exceção sem violações.
Define um mapa vazio de violações.
Chama o método createMessage.
Verifica se a mensagem de exceção está vazia.

5. testExecute_AllFieldsValid_NoExceptionThrown:

Verifica se não há exceção lançada quando todos os campos da fatura são válidos.
Define o status de pagamento como verdadeiro, o valor pago como um BigDecimal positivo e uma mensagem válida.
Executa o método execute.
Não espera que ocorra uma exceção.

7. testExecute_NullFields_ThrowsValidationException:

Verifica se uma exceção é lançada quando campos obrigatórios são nulos.
Define a mensagem como nula.
Executa o método execute.
Espera-se que uma exceção de ValidationException seja lançada.

8. testExecute_EmptyStringFields_ThrowsValidationException:

Verifica se uma exceção é lançada quando campos obrigatórios são vazios.
Define a mensagem como vazia.
Executa o método execute.
Espera-se que uma exceção de ValidationException seja lançada.

9. testExecute_BigDecimalLessThanOrEqualToZero_ThrowsValidationException:

Verifica se uma exceção é lançada quando um campo BigDecimal é menor ou igual a zero.
Define o valor pago e o código do banco como zero ou negativo.
Executa o método execute.
Espera-se que uma exceção de ValidationException seja lançada.
